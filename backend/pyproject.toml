[project]
name = "os-portfolio-backend"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn[standard]>=0.29.0",
    "pydantic>=2.7.0",
]

[tool.vercel]
entrypoint = "main:app"

[tool.vercel.scripts]
build = "python scripts/prepare_vercel.py"
